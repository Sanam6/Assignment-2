#include <iostream>
using namespace std;
int main()
{
    int a, b;
    cout << "Enter the two number a and b : ";
    cin >> a >> b;
    cout << " a < 50 && a < b : " << (a < 50 && a < b);

    return 0;
}